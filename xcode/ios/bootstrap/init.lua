--==============================================================-- Copyright (c) 2010-2011 Zipline Games, Inc. -- All Rights Reserved. -- http://getmoai.com--==============================================================	package.path = "?;?.lua;../?.lua"	----------------------------------------------------------------	-- lua level MOAI sdk initialization	----------------------------------------------------------------	local moai = loadfile( 'moai.lua' )	moai()	----------------------------------------------------------------	-- this function supports all the ways a user could call print	----------------------------------------------------------------	print = function ( ... )				local argCount = select( '#', ... )				if argCount == 0 then			MOAILogMgr.log ( "" )			return		end				local output = tostring ( select( 1, ... ) )				for i = 2, argCount do			output = output .. "\t" .. tostring ( select( i, ... ) )		end				MOAILogMgr.log ( output .. '\n')	end		----------------------------------------------------------------	-- error function that actually prints the error	----------------------------------------------------------------	local superError = error		error = function ( message, level )			print ( "error: " .. message )		print ( debug.traceback ( "", 2 ))		superError ( message, level or 1 )	end	----------------------------------------------------------------	-- lua level yue initialization	----------------------------------------------------------------	yue = require ( 'yue' )